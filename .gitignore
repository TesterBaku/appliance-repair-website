node_modules/
test/screenshots/

# Local task/planning files — owner's working notes, not part of the site
tasks/
*.png
!logo.png
.playwright-mcp/
photo-*.jpeg

# Audit/research screenshots — agents write here; never committed
.audits/

# Temp images — download trial assets here during tasks; cleaned by `npm run clean:images`
images/temp/*
!images/temp/.gitkeep

# Environment and secrets — never commit these
.env
.env.*
!.env.example

# OS / editor artifacts
.DS_Store
Thumbs.db
*.local
.vscode/

# Claude Code local settings — machine-specific, never commit
.claude/settings.local.json

# Credentials and keys
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
secrets.json
secrets.yaml
secrets.yml
config/secrets.*

# CLAUDE.md (local)

All project instructions are canonical in **[../AGENTS.md](../AGENTS.md)** (tracked in the repo).

This local file is intentionally a thin pointer — it previously duplicated AGENTS.md and
drifted (stale Tailwind notes, out-of-date test counts). Do not add guidance here; edit
`AGENTS.md`. `.claude/` is gitignored, so anything put here is local-only and invisible to
other contributors and to scheduled automation runs.

@../AGENTS.md
